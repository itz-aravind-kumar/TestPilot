# Auto-TDD Environment Configuration

# =============================================================================
# HYBRID LLM STRATEGY (RECOMMENDED)
# =============================================================================
# TEST GENERATION: CodeLlama (specialized for test generation via Ollama)
# CODE GENERATION: Gemini (faster, more powerful via Google API)
# =============================================================================

# Test Generation Provider (CodeLlama via Ollama)
TEST_LLM_PROVIDER=ollama
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=codellama:7b
OLLAMA_TEMPERATURE=0.2
OLLAMA_MAX_TOKENS=2048
OLLAMA_TIMEOUT=120

# Code Generation Provider (Gemini via Google API)
CODE_LLM_PROVIDER=gemini
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-1.5-flash
GEMINI_TEMPERATURE=0.0
GEMINI_MAX_TOKENS=2048
GEMINI_TIMEOUT=30

# =============================================================================
# HOW TO GET GEMINI API KEY:
# =============================================================================
# 1. Go to: https://aistudio.google.com/app/apikey
# 2. Click "Create API Key"
# 3. Copy the key and paste it above
# 4. Models available:
#    - gemini-1.5-flash (recommended: fast & cheap)
#    - gemini-1.5-pro (more powerful, slower)
#    - gemini-2.0-flash-exp (experimental, fastest)
# =============================================================================

# =============================================================================
# DOCKER SANDBOX SETTINGS
# =============================================================================
DOCKER_IMAGE=auto-tdd-pytest:latest
MAX_MEMORY=50m
CPU_QUOTA=50000
NETWORK_DISABLED=true
EXECUTION_TIMEOUT=30

# =============================================================================
# REFINEMENT LOOP SETTINGS
# =============================================================================
MAX_ITERATIONS=5
MIN_IMPROVEMENT_THRESHOLD=0.1
CONVERGENCE_PATIENCE=2

# =============================================================================
# RL REWARD SYSTEM
# =============================================================================
REWARD_TEST_PASS=10.0
REWARD_QUALITY_BONUS=5.0
REWARD_EFFICIENCY_BONUS=3.0
PENALTY_SYNTAX_ERROR=-5.0
PENALTY_RUNTIME_ERROR=-3.0
PENALTY_TIMEOUT=-8.0

# =============================================================================
# PATHS
# =============================================================================
ARTIFACTS_DIR=./artifacts
LOGS_DIR=./logs
CACHE_DIR=./.cache

# =============================================================================
# ALTERNATIVE: Use Ollama for BOTH (if no Gemini API key)
# =============================================================================
# CODE_LLM_PROVIDER=ollama
# TEST_LLM_PROVIDER=ollama
# Note: This will be slower but doesn't require API key
